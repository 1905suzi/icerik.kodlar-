const sahne = document.getElementById('sahne'); const yaziSayisi = 65; const satirlar = 4; for (let k = 0; k < satirlar; k++) { const derinlik = (k - 1.5) * 35; for (let i = 0; i < yaziSayisi; i++) { const t = i * (Math.PI * 2) / yaziSayisi; const x = 16 * Math.pow(Math.sin(t), 3) * 14; const y = -(13 * Math.cos(t) - 5 * Math.cos(2*t) - 2 * Math.cos(3*t) - Math.cos(4*t)) * 14; const yazi = document.createElement('div'); yazi.className = 'yazi'; yazi.style.setProperty('--i', i + (k * 10)); yazi.style.left = `calc(50% + ${x}px)`; yazi.style.top = `calc(50% + ${y}px)`; yazi.style.setProperty('--z', `${derinlik}px`); yazi.innerHTML = 'I love you'; sahne.appendChild(yazi); } }