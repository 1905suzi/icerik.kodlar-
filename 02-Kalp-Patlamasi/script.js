const btn = document.getElementById('btn'); const harf = document.getElementById('harf'); const konteyner = document.getElementById('kalp-konteyner'); const renkler = ['#ff007f', '#ff003c', '#e900ff', '#00e1ff', '#ffea00']; btn.addEventListener('click', () => { btn.classList.add('gizle'); harf.classList.add('goster'); for (let i = 0; i < 40; i++) { setTimeout(() => { const kalp = document.createElement('div'); kalp.innerHTML = '❤️'; kalp.classList.add('kalp'); const x = Math.random() * window.innerWidth; const y = Math.random() * window.innerHeight; kalp.style.left = `${x}px`; kalp.style.top = `${y}px`; const rastgeleRenk = renkler[Math.floor(Math.random() * renkler.length)]; kalp.style.textShadow = `0 0 20px ${rastgeleRenk}, 0 0 40px ${rastgeleRenk}`; kalp.style.fontSize = `${Math.random() * 3 + 1}rem`; konteyner.appendChild(kalp); }, i * 40); } });