const canvas = document.getElementById('matrixTuvali'); const ctx = canvas.getContext('2d'); canvas.width = window.innerWidth; canvas.height = window.innerHeight; const harfler = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789@#$%^&*'; const harfDizisi = harfler.split(''); const fontBoyutu = 16; const sutunSayisi = canvas.width / fontBoyutu; const damlalar = []; for (let x = 0; x < sutunSayisi; x++) { damlalar[x] = 1; } function matrixYagdir() { ctx.fillStyle = 'rgba(0, 0, 0, 0.05)'; ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.fillStyle = '#0f0'; ctx.font = fontBoyutu + 'px monospace'; for (let i = 0; i < damlalar.length; i++) { const rastgeleHarf = harfDizisi[Math.floor(Math.random() * harfDizisi.length)]; ctx.fillText(rastgeleHarf, i * fontBoyutu, damlalar[i] * fontBoyutu); if (damlalar[i] * fontBoyutu > canvas.height && Math.random() > 0.975) { damlalar[i] = 0; } damlalar[i]++; } } setInterval(matrixYagdir, 33);