const canvas = document.getElementById('kalpTuvali'); const ctx = canvas.getContext('2d'); canvas.width = window.innerWidth; canvas.height = window.innerHeight; const merkezX = canvas.width / 2; const merkezY = canvas.height / 2; const renkler = ["#ff0000", "#0055ff", "#00ff00", "#ffff00", "#00ffff", "#ff00ff", "#ffa500", "#ffc0cb"]; let i = 0; const toplamCizgi = 120; function kalpCiz() { if (i >= toplamCizgi) return; const aci = i * (Math.PI * 2) / toplamCizgi; const x = 16 * Math.pow(Math.sin(aci), 3); const y = 13 * Math.cos(aci) - 5 * Math.cos(2 * aci) - 2 * Math.cos(3 * aci) - Math.cos(4 * aci); const renk = renkler[Math.floor(Math.random() * renkler.length)]; ctx.strokeStyle = renk; ctx.lineWidth = 1.5; ctx.beginPath(); ctx.moveTo(merkezX, merkezY - 50); const hedefX = merkezX + (x * 15); const hedefY = merkezY - (y * 15) - 50; ctx.lineTo(hedefX, hedefY); ctx.stroke(); ctx.fillStyle = renk; ctx.beginPath(); ctx.arc(hedefX, hedefY, 2.5, 0, Math.PI * 2); ctx.fill(); i++; setTimeout(kalpCiz, 40); } kalpCiz();